# 대표님 할 일 — 2026-09-05 기준

제가 더 못 나가고 **대표님 손이 필요한 것**만 모았습니다. 위에 있을수록 급합니다.
끝낸 건 ☐ 를 ☑ 로 바꾸거나 저에게 한 줄만 주세요. 블로그·사례 내용 답변은 별도 문서 `대표님-5분-답변지.md` 에 있습니다.

## A. 문의 접수함 켜기 (약 30분, 한 번만)

홈페이지 문의가 구글 시트에 이력으로 남고 `lead-inbox.html` 에서 승인·보류·거절을 기록하는 기능입니다. 코드는 배포됐지만 **대표님 구글 계정으로 서버를 한 번 올려야 켜집니다.** 그 전까지는 지금과 완전히 같습니다(메일만).

순서는 `apps-script-lead-inbox/README.md` 그대로입니다.

- ☐ 1. 접수함 전용 구글 스프레드시트 **새로** 만들기 (다른 시트 재사용 금지)
- ☐ 2. script.google.com 새 프로젝트 → `Code.gs`, `LeadInboxPure.gs`, `appsscript.json` 넣기
- ☐ 3. 스크립트 속성 5개 등록 (`LEAD_INBOX_ENABLED`=1, `LEAD_INBOX_SHEET_ID`, `LEAD_INBOX_SESSION_SECRET`, `LEAD_INBOX_LOGIN_PEPPER`, `LEAD_INBOX_ADMIN_CODE`)
  - 관리 비밀번호(`LEAD_INBOX_ADMIN_CODE`)는 8자 이상·공백 없이, **대표님만 아는 값**. 카톡·메모·문서 어디에도 적지 마세요.
  - 선택: `LEAD_INBOX_NOTIFY_TO` 에 메일 주소를 넣으면 새 문의가 올 때마다 그 주소로 알림 메일이 갑니다(Web3Forms 메일이 실패해도 이 메일은 옵니다).
- ☐ 4. 편집기에서 `leadInboxSetupSheets_` 한 번 실행 (권한 승인)
- ☐ 5. 배포 → 새 배포 → 웹 앱 · 실행 사용자 "나" · 액세스 "모든 사용자"
- ☐ 6. 나온 `https://script.google.com/macros/s/…/exec` 주소를 **저에게 전달** → 제가 `data/config.json` 에 넣고 켠 뒤 시험 문의로 끝까지 확인
- ☐ 7. 첫 로그인 뒤 시험 문의는 화면에서 **거절**(사유: 시험)로 정리

## B. 답 한 줄이면 되는 것

- ☐ **문의 수신 메일 주소** — 지금 Web3Forms 가 보내는 대표 메일함이 맞는지, 바꿀 주소가 있는지
- ☐ **네이버 스마트플레이스/예약 인증 코드** — `data/config.json` `naver` 칸이 비어 있어 예약 버튼이 꺼져 있습니다
- ☐ **완성작업위험 보험** 가입 여부·보험사 — 계약서 제11조 옆 표시와 관리사무소 제안서에 들어갑니다 (`docs/문의/보험사-완성작업위험.md`)
- ☐ **견적서 금액 확정** — 관리사무소 표준 패키지(500만 원 이하) 단가표의 빈 금액
- ☐ **도메인** — `01023978629.github.io/manmool` 그대로 갈지, 별도 도메인을 살지(사면 연결은 제가)

## C. 현장 앱 쪽 (hyeonjang)

- ☐ **Watchdog.gs 다시 붙여넣기** — 미수금 독촉을 앱에서 뺐지만(v192) Apps Script 파수꾼은 대표님이 코드를 다시 붙여넣어야 아침 메일이 조용해집니다
- ☐ 앱을 한 번 열어 **v259** 인지 확인 (더보기 → 버전). v255 부터는 새 버전이 나오면 앱을 열 때 「새 버전」 배너가 뜹니다 — 「지금 새로고침」 한 번이면 됩니다(저장 안 한 변경이 있으면 먼저 저장하라고 막습니다). 배너가 안 뜨면 앱을 완전히 닫았다 다시 여세요
- ☐ **자재 거래처·구매 링크·가격 정리는 🛒 자재 구매처·단가로** (v258) — 더보기 → 🛒. 자재마다 구매처 여러 곳(거래처·쇼핑몰 링크·가격·수량·확인일)을 넣으면 싼 순으로 정렬돼 「최저」가 보이고, 「📋 최저가 목록 복사」로 문자·카톡에 붙일 수 있습니다. 링크를 붙여넣으면 쇼핑몰 이름은 자동으로 채워집니다
- ☐ **견적 쓸 때 자재 단가는 「🛒 자재」로** (v259) — 견적 작성 품목 줄의 「🛒 자재」를 누르면 정리한 자재가 뜨고, 누르는 대로 품목에 들어갑니다(수량 1, 쇼핑몰 가격은 부가세를 뺀 공급가로). 마진은 단가를 고쳐 얹으세요
- ☐ **폰에서 사진 한 장 도구는 ⋯ 버튼** (v256) — 사진 칸 오른쪽 위 ⋯ 를 누르면 이동·이름·삭제 큰 버튼이 뜹니다. 사진 분류 화면의 추천 버튼은 목록 위 요약 줄로 올라갔습니다(v254)
- ☐ **새 사진은 👉 추천 배정으로** (v253) — 사진 탭 맨 위 미지정 묶음에 「👉 현장명 배정 (근거)」 버튼이 뜹니다. 한 번 누르면 그 묶음의 현장 없는 사진만 들어갑니다. 여러 묶음이면 목록 위 「✨ 추천대로 전부 배정」 한 번(안전판을 먼저 만듭니다). 「참고:」가 붙은 제안은 GPS 없는 사진이라 직접 확인 후 누르세요
- ☐ **사진 배정 점검 한 번 돌리기** (v252, 약 2분) — 더보기 → 📍 사진 배정 점검
  1. 「재배정 제안」에 체크된 사진을 훑어보고 「체크한 사진 재배정」 (안전판이 먼저 만들어집니다)
  2. 「확인 필요」 사진은 가까운 현장이 없어 자동으로 옮기지 않습니다 — 사진 탭에서 직접 현장을 고르거나 그대로 두세요
  3. 「중복 N장 제거」 → 대표 1장만 남깁니다(드라이브 원본은 그대로)
  4. 끝나면 저장(💾)을 눌러 주세요. 되돌리려면 더보기 → 🛡 안전판에서 「사진 배정 점검 전」을 여세요
  - 사진 원본·현장 데이터가 대표님 PC·드라이브에만 있어 제가 대신 돌릴 수 없습니다. GPS 없는 사진은 위치로 판단하지 못합니다
- ☐ **(대신 해 드리려면) 현장 데이터를 저에게 주세요** — 둘 중 하나
  - 앱 → 더보기 → 데이터 → **내보내기**로 `현장데이터.json` 을 받아 이 대화에 올리기
  - 또는 구글드라이브 **만물인테리어** 폴더(앱이 쓰는 폴더, ID `1VRtT1ACNgMZiNixABpUPENn5fWFXvUoT`)를 `dudejr267@gmail.com` 에 **보기** 권한으로 공유하기
  - 받으면 `tools/photo-audit-offline.mjs`(앱과 같은 규칙)로 점검해 **보고서 + 수정본 JSON** 을 드리고, 대표님은 앱 → 데이터 → 불러오기로 열기만 하면 됩니다(앱이 먼저 백업을 묻습니다). 중복 제거는 앱에서만 '다시 불러와도 안 들어오게' 기록되므로 앱 버튼으로 합니다

## D. 이미 끝난 것 (참고)

- 6자리 번호와 관리 비밀번호 이름을 **'비밀번호'**로 통일 (사이트 #154, 앱 v248)
- 관리사무소 페이지 1순위 버튼을 **시설보수 접수**로 (#154)
- 문의 접수함 코드·검사·처리방침 (#155) — A 만 남았습니다
- 접수함 보관기한 자동 삭제(거절 90일·승인 1년, #173) — 접수함을 켜면 새벽 4시 정리가 함께 돕니다
- 첫 화면 카톡 버튼·주 버튼 색 대비·건너뛰기 링크 (#173·#175)
- 검사 흔들림 정리 — 느린 서버에서 한 번 떨어진 원인 수정, 두 저장소 검사 전체 점검 (#176, 앱 #104). 하실 일 없음
