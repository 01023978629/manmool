# 코덱스 인수인계서 — 사례 등록 흐름과 상담 폼

> 작성 2026-08-12. 대상: 이 일을 이어받는 코덱스(또는 다른 에이전트).
> 상시 규칙은 `AGENTS.md`, 대표가 직접 해야 하는 몫은 `CODEX-인수인계.md`(2026-08-05).
> **여기는 오늘 새로 생긴 구조와 그 위에서 지켜야 할 것을 다룬다.**

---

## 0. 지금 상태

- `main` = `277ba46` · 공개 주소 `https://01023978629.github.io/manmool/`
- 오늘 배포 5건 (#108 ~ #112). 전부 병합·배포 완료, 검사 전부 통과.
- **비밀값은 이 문서에 없다.** 앞으로도 넣지 마라. 이 저장소는 루트 전체가
  공개 서빙되므로 이 문서 자체도 인터넷에서 읽힌다.

| PR | 내용 |
|---|---|
| #108 | 누수 페이지 전용 상담 폼 · 모바일 헤더 전화 버튼 44px |
| #109 | 사례 등록 화면 `case-new.html` |
| #110 | 한밭유성아파트 글 보강(사진 3장 · 내시경 과정) |
| #111 | 저장 목록(IndexedDB) |
| #112 | 저장한 현장을 다른 기기로 옮기기 |

---

## 1. 오늘 생긴 구조 — 건드리기 전에 읽어라

새 파일 6개가 생겼다. 셋은 **"같은 규칙을 두 곳에 적지 않기 위해"** 뽑아낸
공용 모듈이다. 편의상 만든 게 아니라, 갈라지면 사고가 나서 합친 것이다.

### `js/pii-rules.js` — 개인정보 판별 규칙 정본

동·호수, 휴대전화, 일반전화, 이메일, 고객명 표기를 잡는 정규식 목록.

- 브라우저: `<script>` 로 읽어 `window.MANMUL_PII_RULES`
- Node: `scripts/new-case-post.mjs` 가 **이 파일을 읽어 `node:vm` 으로 평가**

**왜 이렇게까지 했나.** 규칙을 화면과 생성기에 따로 적으면 반드시 갈라진다.
그러면 등록 화면은 "괜찮다"고 통과시켰는데 실제 글에는 동·호수가 남는다.
사본을 만들 수 없는 구조로 두는 것이 유일하게 확실한 방법이다.

> **규칙을 고칠 일이 있으면 이 파일만 고쳐라.** 다른 곳에 정규식을 다시
> 적으면 `scripts/ensure-case-new.mjs` 가 잡는다.

**알려진 오탐 하나.** 공개 주소 `01023978629.github.io` 의 숫자가 휴대전화
규칙에 걸린다. 대표 본인의 사업용 번호이자 GitHub 계정명이라 이미 사이트
곳곳에 공개돼 있다 — 고객 개인정보가 아니다. 문서나 설정 파일을 검사할 때
이 항목이 뜨면 주소 때문인지 먼저 확인하고, 규칙을 느슨하게 풀지 마라.

### `js/lead-transport.js` — 상담 리드 전송·보관 정본

인테리어 폼(`index.html`)과 누수 폼(`leak.html`)이 같이 쓴다.
담고 있는 것은 세 가지 — **어디로 보내는가 · 얼마나 보관하는가 ·
사람이 읽는 형식**.

- `deliver(config, payload)` — n8n 우선, 없으면 web3forms, 둘 다 없으면 `false`
- `RETENTION_DAYS = 365` — **화면의 "보유기간 1년" 안내와 반드시 같아야 한다**
- `buildLeadText(payload)` — 문자·카톡으로 넘길 본문

**왜.** 폼마다 전송 코드를 따로 두면, n8n 주소를 바꿀 때 한쪽만 고쳐져서
그쪽 리드가 조용히 샌다. 보유기간도 마찬가지로 안내와 코드가 어긋난다.

### `js/case-store.js` — 저장한 현장 보관소 (IndexedDB)

`localStorage` 가 아니다. 문자열만 담기고 한도가 5MB 안팎이라
**사진 몇 장이면 저장이 통째로 실패**한다. 그러면 대표가 적어둔 현장이 사라진다.

### 나머지

- `js/leak-inquiry.js` — 누수 전용 상담 폼. 평수·예산·공사범위를 묻지 않는다.
- `js/case-new.js` + `css/case-new.css` + `case-new.html` — 사례 등록 화면.

---

## 2. 사례 등록 흐름 — 누가 무엇을 하나

```
[대표]  현장 끝날 때마다 case-new.html 에서 6항목 + 사진 → [이 현장 저장]
          ↓  (휴대폰 → PC 는 [다른 기기로 넘기기] 파일 한 개로)
[대표]  편할 때 [밀린 것 전부 복사] → 대화창에 붙여넣기 + 사진 첨부
          ↓  "밀린거 해줘"
[코덱스] 개인정보·EXIF 재확인 → 글·블로그 목록·RSS·사이트맵 → 배포 → 보고
          ↓
[대표]  각 건에 [올림 표시] → 쌓이면 [올린 것 지우기]
```

### 대표 결정으로 확정된 것 (2026-08-12)

- **"올리기" 버튼은 만들지 않는다.** 브라우저에 GitHub 토큰을 두는 방식은
  대표가 거절했다. 되살리지 마라.
- **승인 단계는 두지 않는다.** 재료를 받으면 바로 올리고 결과를 보고한다.
- 기기 간 자동 동기화는 **보류**. 파일로 옮기는 방식으로 당분간 쓴다.
  (구글 드라이브 · n8n 두 안을 제시했고 대표가 "일단 이대로" 를 택했다.)

### 재료를 받았을 때 코덱스가 하는 일

1. `piiFindings()` 로 본문·대체텍스트·설명 전체를 다시 본다.
   화면에서 걸렀더라도 다시 본다 — 옛 화면에서 왔을 수 있다.
2. 사진 EXIF 를 지운다(PIL 재인코딩). 긴 변 1600px, 품질 82 안팎.
   **원본은 건드리지 않는다.**
3. **이미 올라간 현장인지 먼저 본다.** 같은 공사로 글을 하나 더 만들면
   검색에서 중복으로 잡힌다. 판정은 축소 그레이스케일 평균 밝기차로 한다
   (8 미만이면 같은 사진). 실제로 #110 에서 넉 장 중 한 장이 이미 올라간
   사진이었다 — 그대로 만들었으면 중복 글이 생겼다.
4. 기존 글이면 보강한다. 새 현장이면 `data/site.json` 의 `insights` 에 넣고
   `scripts/prerender-posts.py` 를 돌린다(글·목록·RSS 가 한 번에 나온다).
5. 내용이 바뀌었으면 `sitemap.xml` 의 `lastmod` 를 올린다.
   안 올리면 네이버·구글이 다시 읽을 이유가 없다.

### 글 본문에 사진 붙이기 (#110 에서 추가)

`insights[].body[]` 의 각 절에 `img` · `imgAlt` · `imgCaption` 을 **선택으로**
줄 수 있다. 해당 문단 바로 아래에 근거 사진이 붙는다. 없으면 예전과 같다.

---

## 3. 검사 체계 — 여기가 이 저장소의 핵심이다

`scripts/ensure-*.mjs` 12개. 배포 전 전부 통과해야 한다.

```bash
for f in scripts/ensure-*.mjs; do timeout 120 node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node scripts/new-case-post.test.mjs
```

오늘 추가된 둘:

- `ensure-leak-inquiry.mjs` — 누수 폼 필수 요소, 스크립트 순서, 폼별 자체
  전송 구현 금지, 화면 안내와 실제 보유기간 일치, 접수 뒤 폼 숨김, 헤더 44px
- `ensure-case-new.mjs` — 규칙 단일 출처, 규칙 사본 금지, 재료 라벨 일치,
  미리보기와 실제 글의 틀 일치, noindex·robots, **전송 코드 금지**,
  저장 실패 처리 순서, 파일 이름 한글 금지, 받을 때 재검사·중복 방지

### 규율: 검사를 추가하면 반드시 변이 검증을 하라

**검사를 짜 놓고 통과하는 것만 확인하면 아무것도 보장하지 못한다.**
지키려는 동작을 일부러 망가뜨려서 검사가 **실패하는지** 확인해야 한다.

오늘 이 방법으로 **내가 짠 검사에서 결함 5건**을 잡았다. 전부 같은 종류다 —
**부분 문자열 매칭**:

| 짠 것 | 왜 통과했나 |
|---|---|
| `/ManmulLead/` | `ManmulLeadX` 도 통과 |
| `/d\.type/` | `payload.type` 의 뒤쪽에 걸림 |
| `/pii-rules\.js/` | 설명 **주석**에만 이름이 있어도 통과 |
| 저장 처리의 "첫 `return`" | 맨 앞 `if (blocked(g)) return;` 이 먼저 걸림 |
| `download(...)` 인자 정규식 | 첫 인자 안의 쉼표에서 매칭이 끊김 |

> 새 검사는 **이름이 어딘가에 나오는지**가 아니라 **실제로 그렇게 동작하는지**
> 를 보게 짜라. 단어 경계(`\b…\b`), 실제 호출(`readFileSync(…)`),
> 중괄호 균형으로 블록 잘라내기를 써라.

---

## 4. 절대 하지 마라

1. **개인정보 정규식을 `js/pii-rules.js` 밖에 다시 적지 마라.**
2. **전송·보관 코드를 폼별로 다시 만들지 마라** — `js/lead-transport.js` 하나다.
3. **`case-new.html` 에서 어디로든 전송하지 마라.** `fetch`·XHR·`sendBeacon`·
   폼 `action` 전부 금지다. 검사가 잡는다. "사진을 서버에 올려두면 편하다"는
   유혹이 오겠지만, 이 화면은 아무것도 내보내지 않는다는 약속 위에 서 있다.
4. **내려받는 파일 이름에 한글을 쓰지 마라.** 브라우저가 이름을 통째로 버리고
   확장자 없는 `download` 로 저장한다 → PC 파일 고르기에 안 뜬다 → 흐름이 끊긴다.
5. **저장이 실패했는데 입력을 비우지 마라.** 대표가 적어둔 현장이 그냥 없어진다.
   `STORE.put` 이 성공한 뒤에만 비운다.
6. **브라우저에 GitHub 토큰을 두지 마라.** 대표가 명시적으로 거절했다.
7. **화면의 보유기간 안내와 `RETENTION_DAYS` 를 따로 고치지 마라.**
8. **실제 web3forms 로 시험 접수를 보내지 마라.** 대표 메일함이 더러워진다.
   테스트할 때는 `page.route('**://api.web3forms.com/**', …)` 로 가로채라.
9. **완료하지 않은 것을 완료로 적지 마라.**
10. **PR 을 병합하기 전에 base 가 `main` 인지 확인하라.** GitHub 은 부모
    브랜치를 기본값으로 잡는다. 이걸 놓쳐서 배포되지 않은 걸 배포됐다고
    보고한 적이 있다(#96 → #97).

---

## 5. 남은 일

### 코덱스 몫

- [ ] `js/main.js` 가 `site.json` 값을 템플릿 문자열 `innerHTML` 로 그린다.
      `escapeHtml` 이 없다. `site.json` 을 자동으로 고치는 경로가 생기면
      저장형 XSS 가 된다. 지금은 사람만 고치므로 급하지 않지만, 남아 있다.
- [ ] 누수 FAQ 가 4개다(인테리어는 12개). 대표에게 자주 받는 질문을 받아 늘려라.
- [ ] `leak.html` 실제 누수 사례가 1건뿐. 사례가 쌓이면 채워라.
- [ ] 병합되지 않은 코덱스 브랜치 정리:
      `codex/install-rehearsal-20260810`, `codex/site-defects-20260810`,
      `codex/leak-pricing-ready-20260810`, `codex/case-post-pipeline-20260810`,
      `codex/third-wave-main-20260810`

### 대표 몫 (코덱스가 대신 못 한다)

- [ ] **네이버 소유확인 코드** — 저장소에 확인 파일·메타가 없다. 확인이 풀릴 수
      있다. 코드를 받으면 `scripts/set-verification.mjs` 로 넣어라.
- [ ] 한밭유성 글 **재수집 요청**(#110 으로 내용이 바뀜)
- [ ] 누수 페이지 상담 폼 **시험 접수** → 메일 도착 확인
- [ ] `leak.html` 을 대문으로 올릴지 결정 — 네이버 색인이 자리 잡는
      **2~3주 뒤**를 권고했다. 지금 바꾸면 갓 등록한 색인이 흔들린다.

---

## 6. 확인 방법

```bash
# 검사 전체 + 단위 테스트
for f in scripts/ensure-*.mjs; do timeout 120 node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node scripts/new-case-post.test.mjs

# 글·목록·RSS 다시 만들기 (site.json 을 고친 뒤)
python3 scripts/prerender-posts.py

# 미리보기 (브라우저 확인용)
python3 -m http.server 8777
```

브라우저 확인은 `/opt/node22/lib/node_modules/playwright` 를 쓴다.
**IIFE 밖에 `browser` 를 두고 `catch` 에서 닫아라** — 안에 두면 예외가 났을 때
브라우저가 살아남아 프로세스가 끝나지 않고 **그대로 멈춘다**.

---

## 7. 마지막

이 저장소에서 가장 값어치 있는 것은 기능이 아니라 **검사와 변이 검증**이다.
1인 사업자의 홍보 사이트라 잘못 올라간 것을 아무도 대신 발견해 주지 않는다.
고객 동·호수가 글에 남거나, 상담이 접수되는 줄 알았는데 아무 데도 안 가거나,
사진에 집 좌표가 박혀 나가는 일 — 전부 조용히 벌어지고 몇 달 뒤에 발견된다.

검사를 늘려라. 그리고 늘린 검사가 **정말 잡는지** 망가뜨려서 확인해라.
