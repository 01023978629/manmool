# 코덱스 인수인계서 — 대표가 하던 몫

> 작성 2026-08-05. 대상: 이 일을 이어받는 코덱스(또는 다른 에이전트).
> 저장소별 상시 규칙은 `AGENTS.md`(이 저장소)와 `hyeonjang/AGENTS.md` 에 있다.
> **여기는 "대표가 직접 해야 했던 일"만 다룬다.**

---

## 0. 먼저 읽어야 할 것 — 이 인수인계의 한계

대표 지시는 "내가 할 일을 코덱스가 할 거니 인수인계서를 써 달라"였다.
그대로 받아 적으면 거짓말이 되므로 먼저 분명히 해 둔다.

**아래 일의 상당수는 코덱스도 못 한다.** 구글 계정 로그인, 콘솔 클릭,
결제수단 확인, 문자 발송 승인은 사람의 신원과 손이 필요하다. 코덱스가
브라우저를 자동화할 수 있는 환경이라 해도, 구글은 자동화된 키 발급 시도를
막는다(대표가 이미 `The request is suspicious` 로 막혔다 — 바로 그 방어다).

그래서 각 항목을 **"코덱스가 실제로 할 수 있는 몫"** 과
**"사람 손이 반드시 필요한 몫"** 으로 쪼개 두었다.
코덱스는 앞쪽을 최대한 밀어 두고, 뒤쪽은 **대표가 3분 안에 끝낼 수 있게
준비된 상태로** 넘겨라. 그것이 여기서 가능한 최선이다.

**절대 하지 마라: 사람 손이 필요한 칸을 "완료"로 적는 것.**
안 된 것을 됐다고 적으면 대표는 확인을 멈추고, 그 상태로 몇 달이 간다.
(실제로 그래서 백업 0건이 몇 달 뒤에야 발견됐다. §5 참고)

---

## 1. Gemini API 키 발급 — 🔴 막힘, 최우선

### 상황
- 새 프로젝트 `Manmool Gemini No Billing` 생성 완료, 결제계정 분리 완료,
  Gemini API 활성화 완료, AI Studio 임포트 완료.
- **마지막 한 걸음에서 막힘**: `aistudio.google.com/api-keys` 에서
  `Failed to generate API key, The request is suspicious. Please try again.`

### 왜 이 키가 필요한가
앱의 AI 비서·사진 공정 태깅·견적 초안이 이 키로 돈다. 없으면 그 기능들이
전부 잠긴 채로 남는다. 다만 **앱은 키 없이도 정상 동작한다** — 급하지만
앱을 멈추는 문제는 아니다.

### 코덱스가 할 수 있는 몫
- 없음. 키 발급은 로그인된 사람의 브라우저에서만 된다.

### 사람 손이 필요한 몫 (대표)
1. `aistudio.google.com/api-keys` 접속
2. 프로젝트 `Manmool Gemini No Billing` 선택
3. [API 키 만들기]

`suspicious` 가 또 나오면 순서대로 시도:
- 시크릿 창 해제 후 일반 창에서 (VPN·프록시 끄고)
- 다른 네트워크(휴대폰 테더링)
- 몇 시간 뒤 재시도 — 이 오류는 대개 일시적 속도제한이다
- 그래도 안 되면 브라우저 캐시·쿠키 삭제 후 재로그인

### 🔴 발급 후 절대 금지
- **키를 코드·커밋·채팅·로그 어디에도 넣지 마라.** 앱 ⚙️설정 화면에
  대표가 직접 붙여 넣는다. 키는 기기 IndexedDB 에만 산다.
- 결제계정을 이 프로젝트에 다시 연결하지 마라 — 무료 한도를 쓰려고
  일부러 떼어 놓은 것이다.

### 완료 판정
앱 ⚙️설정 → AI·지도 키 탭 → Gemini 칸 옆 [확인] 버튼이 초록으로 통과.
**대표가 이 화면을 보고 확인해 줘야 완료다. 추정으로 적지 마라.**

---

## 2. 전자계약 Apps Script 서버 설치 — 🟡 대기

### 상황
코드·테스트·설치안내 **완성**. `apps-script-contract/` 가 소스다.
여기 커밋해도 배포되지 않는다 — 대표가 자기 구글 계정에 붙여넣어야 한다.

### 코덱스가 할 수 있는 몫
- `apps-script-contract/SETUP.md` 절차를 실제 파일 목록과 대조해 검증하고,
  낡았으면 고쳐라. **대표가 막히는 지점은 대부분 안내서가 낡아서다.**
- 붙여넣을 파일 순서·Script Properties 키 목록을 한 장으로 정리해 둬라.
- `node --test apps-script-contract/test/*.mjs` (227건) 가 통과하는지 확인.
- `수동검증-체크리스트.md` 를 실제 화면 흐름과 맞춰 갱신.

### 사람 손이 필요한 몫 (대표)
`SETUP.md` 대로 Apps Script 프로젝트 생성 → 파일 붙여넣기 →
Script Properties 에 비밀값 입력 → 배포 → 배포 URL 을 앱 ⚙️설정에 입력.

### 🔴 절대 금지
- **`ALIMTALK_LIVE` 를 켜지 마라.** 실제 문자 발송은 계속 OFF 가 정상이며,
  selfTest 는 그게 켜져 있으면 **일부러 실패한다.**
- **비밀값(`ADMIN_TOKEN`·`PEPPER`·Solapi 키)을 저장소에 넣지 마라.**
  이 저장소 루트는 통째로 공개 웹에 서빙된다.
- 은퇴한 `manmool-contract.fly.dev` 를 되살리지 마라.
  `contract-backend/` 는 참고용 보관이니 지우지도 마라.

### 완료 판정
앱 ⚙️설정 → 전자계약 탭 → **자가진단 통과**. 통과 전까지 계약 버튼은
잠긴 채로 두는 것이 정상이다. 잠금을 코드로 우회하지 마라.

---

## 3. 네이버·구글 사이트 소유확인 — 🟢 코덱스가 절반 할 수 있음

### 상황
`index.html` 11~14행에 **주석 처리된 자리표시자**가 있다.
```html
<!-- 검색엔진 소유 확인: 발급받은 코드로 content를 채워 주석을 해제하세요
<meta name="naver-site-verification" content="네이버_서치어드바이저_발급코드" />
<meta name="google-site-verification" content="구글_서치콘솔_발급코드" />
-->
```

### 코덱스가 할 수 있는 몫
대표가 발급코드 두 개를 문자로 알려 주면, **주석을 풀고 값을 채워
커밋·PR 까지** 코덱스가 한다. 절차는 `integrations/GROWTH.md`.

### 사람 손이 필요한 몫 (대표)
네이버 서치어드바이저·구글 서치콘솔에서 사이트 등록 → 발급코드 복사.
사이트 주소는 `https://01023978629.github.io/manmool/`.

### 주의
이 코드는 비밀값이 아니다(어차피 HTML 에 공개된다). 커밋해도 된다.
**단, 값을 지어내지 마라.** 틀린 코드를 넣으면 소유확인이 실패하고
대표는 왜 안 되는지 모른 채 시간을 버린다.

---

## 4. Threads 토큰 재발급 — 🟡 낮은 우선순위

`@moneyrabbit__` 자동 포스팅 토큰이 **2026-06-19 만료**됐다.
Meta 개발자 콘솔에서 대표가 재발급해야 한다. 코덱스가 할 몫은 없다.
홍보 자동화가 멈춘 것뿐이라 본업에 지장은 없다 — 1~3번을 먼저 끝내라.

---

## 5. 서버 날짜별 백업 0건 — 🟢 원인 규명 완료, 조치 완료

**이 항목은 "대표가 할 일"로 잘못 분류돼 있었다.** 원인을 밝혔으니 기록해 둔다.

### 무엇이 문제였나
드라이브에 백업 파일이 **0건**인 것을 몇 달 뒤에야 발견했다.
코드를 따라가 보니 앱쪽 경로(`relaySaveNow` → `relayDailyBackup` →
`cloudApiBackup`)도, 서버쪽 `makeBackup_` 도 **멀쩡했다.**

진짜 원인은 `relayDailyBackup` 이 모든 실패를 `catch(e){}` 로 삼켰다는 것이다.
매일 조용히 실패해도 그것을 알려 줄 화면이 **하나도 없었다.**
백업 센터는 로컬 스냅샷만 보여 준다.

### 조치 (앱 v179)
성공·실패를 모두 기록하고 백업 센터에 표시한다. 실패면 사유와
다음에 뭘 해야 하는지까지 띄운다. `tests/backup-visible.e2e.js` 가 지킨다.

### 대표가 확인해야 할 것
앱 → 더보기 → 💾 백업 센터. 거기 새로 생긴 줄을 보고 알려 달라:
- **초록(성공)** — 정상. 끝.
- **빨강(실패)** — 사유를 그대로 알려 달라. 대개 "서버에 한 번도 저장한
  적이 없어 백업할 원본이 없음"이며, 그러면 [☁️ 서버에 저장하기] 한 번이면 된다.
- **회색(기록 없음)** — 아직 백업이 시도된 적 없음. 서버 저장이 한 번
  성공하면 그다음부터 하루 1회 자동으로 남는다.

---

## 6. 코덱스가 지금 바로 할 수 있는 일 (승인 불필요)

사람을 기다리지 않고 진행 가능한 것들. 저장소 안에서 끝난다.

| 일 | 저장소 | 비고 |
|---|---|---|
| `SETUP.md`·`수동검증-체크리스트.md` 를 실제 코드와 대조해 갱신 | manmool | §2 의 걸림돌을 미리 없앤다 |
| 데모 데이터에 아파트 오더 표본 추가 | hyeonjang | 새 기능이 빈 화면으로 보인다 |
| 블로그 글 추가 (3곳 동시 등록) | manmool | `posts/` + `blog.html` + `sitemap.xml` |

**각 변경의 필수 절차는 해당 저장소 `AGENTS.md` 를 따라라.**
요약하면: 새 기능 = 새 테스트, 반드시 **변이 검증**(보호 동작을 일부러
되돌려 테스트가 실패하는 것까지 확인), `index.html` 고쳤으면 `sw.js`
캐시 버전 올리기, main 직접 push 금지.

---

## 7. 무슨 일이 있어도 하지 마라

1. **비밀값을 저장소·로그·채팅에 남기기.** manmool 루트는 통째로 공개된다.
2. **고객에게 실제 발송.** 문자·알림톡은 이중으로 꺼져 있고 그게 정상이다.
   `notify.sent === true` 가 아니면 "보냈다"로 기록하지 마라.
3. **`hyeonjang/apps-script/` 수정.** 운영 중인 사진 중계 서버 소스다.
   전자계약 서버는 저기가 아니라 manmool `apps-script-contract/` 다.
4. **자가진단 통과 전에 계약 기능 열기.**
5. **검증 없이 완료 보고.** 특히 §1·§2 는 대표 눈으로 확인해야 완료다.
6. **대표의 기존 변경 되돌리기** — `contract-backend/bin/preflight.mjs`,
   `assets/design-detail-sheets/`.

---

## 8. 우선순위

1. **§6** — 사람을 안 기다리는 일부터. 지금 바로.
2. **§1 Gemini 키** — 대표 3분. 막히면 §1 의 시도 순서대로.
3. **§2 전자계약 설치** — 대표 30분. §6 에서 안내서를 먼저 손봐 두면 짧아진다.
4. **§5 백업 확인** — 대표 1분. 화면 보고 색만 알려 주면 된다.
5. **§3 소유확인 코드** — 대표 10분, 나머지는 코덱스.
6. **§4 Threads** — 급하지 않다.
